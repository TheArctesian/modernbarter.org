import { expect, test } from '@playwright/test';
 
// I really should be writing these firsts because all the studies show that it speeds up production but you known I am camus and also don't know how I want to build this yet so, vive la absurd



//test('index page has expected h1', async ({ page }) => {
//	await page.goto('/');
//	expect(await page.textContent('h1')).toBe('Welcome to SvelteKit');
//});
